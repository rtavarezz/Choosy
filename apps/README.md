# Apps

This folder is for microservices or app-specific logic that is not part of the main backend or frontend.

- `api/`: App-specific API logic (e.g., auth microservice).
- `web/`: Reserved for future web microservices.

## Usage

Use this folder for additional apps/services that are not part of the main backend/frontend structure.
