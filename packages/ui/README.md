# Shared UI Components

This folder contains UI components shared across the monorepo (frontend, backend, etc).

- `AuthModal.tsx`: Shared authentication modal.
- `index.ts`: Exports for shared UI components.

## Usage

Import shared UI components from here to keep UI consistent and DRY.
