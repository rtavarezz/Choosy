# Shared Packages

This folder contains code shared across the monorepo (backend, frontend, etc).

- `database/`: Shared DB logic or schema.
- `ui/`: Shared UI components.
- `shared/`: Shared types, utilities, or logic.

## Usage

Import shared code from these packages to avoid duplication and keep logic DRY.
